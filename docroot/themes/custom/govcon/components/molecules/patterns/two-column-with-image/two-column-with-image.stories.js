import htmlComponent from './two-column-with-image.twig';

export default {
  title: "Molecules/Patterns/Section/Two Column With Image"
}

const Component = (args) => htmlComponent(args);

export const Default = Component.bind({});
