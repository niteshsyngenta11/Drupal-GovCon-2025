import { Meta, Story, Canvas, ArgTypes } from "@storybook/blocks";
import logoStories from "./logo.stories";
import logoTwig from "./logo.twig";

<Meta title="Atoms/Logo" of={logoStories} />

# Logo

The Logo component is used to display different variations of the brand logo.

## Variants

### Primary Logo

The primary logo variant.

<Canvas>
  <Story name="Primary">
    {logoTwig({ src_path: "/assets/images/Default.png", src_alt: "Logo" })}
  </Story>
</Canvas>

### Secondary Logo

The secondary logo variant.

<Canvas>
  <Story name="Secondary">
    {logoTwig({ src_path: "/assets/images/Blue.png", src_alt: "Logo" })}
  </Story>
</Canvas>

### Default Logo

The default logo variant.

<Canvas>
  <Story name="Default">
    {logoTwig({ src_path: "/logo.svg", src_alt: "Logo" })}
  </Story>
</Canvas>

## Props

<ArgTypes of={logoStories} />
